singularity {
        enabled = true
        runOptions = '-B /beegfs -B /homes --nv'
        envWhitelist = 'SINGULARITY_BINDPATH, SINGULARITY_LD_LIBRARY_PATH'
    }

// Run Locally

    process {
        executor = 'local'
    }